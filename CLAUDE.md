# siki-dream-rich 프로젝트 규칙

## ❌ 절대 금지 사항

### 데이터 저장
- `localStorage` 사용 절대 금지
- `sessionStorage` 사용 절대 금지
- 하드코딩된 mock 데이터 사용 금지
- 인메모리 임시 상태로 데이터 저장 금지 (새로고침 시 날아가는 구조 금지)

### 코드 품질
- 더미 데이터로 UI만 먼저 만들고 DB 연결 나중에 하는 방식 금지

---

## ⚡ 실행 자율성

- 매번 "진행할까요?" "계속할까요?" 같은 확인 없이 바로 실행할 것
- 아래 작업은 승인 없이 자동 실행:
  - 파일 생성 / 수정 / 삭제
  - `npm install`, 패키지 추가
  - Supabase 마이그레이션 실행
  - Vercel 배포 (`vercel --prod`)
- 작업 완료 후 결과만 간단히 보고할 것

---

## ✅ 데이터 저장 원칙

- **모든 읽기/쓰기는 Supabase client를 통해서만 처리**
- 새 기능 추가 시 → Supabase 테이블 설계 먼저, 구현 나중
- 기기 간 데이터 동기화가 핵심이므로 항상 서버 데이터 기준으로 렌더링
